<div class='form-group'>
  <label for={{@for}}>{{@labelTitle}}</label>
  <input
    type={{@type}}
    name={{@name}}
    id={{@id}}
    placeholder={{@placeholder}}
    value={{@value}}
  />
  <small>Error message</small>
</div>